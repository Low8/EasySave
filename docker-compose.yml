services:
  easysave-server:
    build:
      context: .
      dockerfile: EasySaveServer/Dockerfile
    ports:
      - "9090:9090"
    environment:
      - EASYSAVE_DATA_DIR=/data
      - EASYSAVE_SOCKET_PORT=9090
      - EASYSAVE_CONTAINER_HOST_ROOT=/host
    volumes:
      - ./server-data:/data
      - type: bind
        source: C:\
        target: /host/C
